import React from 'react';
import Container from './ui/Container';

const Footer: React.FC = () => {
  return (
    <Container color={'lightGray'} bgColor={'darkGray'} padding="1rem">
      <p>footer works!</p>
    </Container>
  );
};

export default Footer;
